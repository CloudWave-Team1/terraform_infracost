version: 0.1
projects:
{{- range $project := matchPaths "env/prod/:main.tf" }}
  {{- if ne $project.env "legacy"}}
    - path: .
      name: {{ $project.env }}
      terraform_var_files:
        - {{ $project._path }}
  {{- end}}
{{- end }}